{
  "name": "@ulkajs/plugin-sass",
  "version": "1.0.0",
  "description": "Use sass with ulkajs",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "cb": "run-s clean build",
    "build": "tsc",
    "clean": "rimraf dist",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s --commit-path . --lerna-package plugin-sass",
    "release": "./scripts/update && ../../scripts/release --skipBuild"
  },
  "devDependencies": {
    "@types/node": "^15.3.0",
    "@types/sass": "^1.16.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  },
  "peerDependencies": {
    "sass": "^1.32.13",
    "ulka": ">=1.0.0"
  }
}
